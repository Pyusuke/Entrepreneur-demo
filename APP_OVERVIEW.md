# 探究振り返りカードゲーム — アプリ概要（引き継ぎ用）

このドキュメントは、このリポジトリのアプリを初見で理解するための説明書です。
「何のためのアプリか」「どう作られているか」「触るときに踏みやすい地雷」を1枚にまとめています。

---

## 1. 一行で言うと

高校3年生が、自分の過去の経験を「手札（カード）」にして、これからの選択に切ってみる **授業用のWebアプリ**。
勝敗も点数もなく、アプリは一切の判定をしない。**対話の道具**であって、ゲームの審判ではない。

---

## 2. 使われる場面（これを知らないと設計判断を誤る）

アントレプレナーシップ教育のお試し授業（全120分）の中核、65分のワークショップで使う。

| 時間 | パート |
|---|---|
| 0〜15分 | 導入（アントレプレナーシップとは／探究との関係） |
| 15〜80分 | **ワークショップ＝このアプリを使用** |
| 80〜90分 | 休憩 |
| 90〜120分 | 振り返り（授業者の実体験→個人→班→全体。まとめ画面を見ながら実施） |

- 対象：高校3年生（夏時点）。1人1台端末（学校のタブレット / Chromebook 想定、スマホでも動く）
- 進行：**ファシリテーター（先生）の合図で生徒が各自進む**。だから画面遷移は手動で、かつパスワードで先走りを止めている
- 生徒に持ち帰らせたい体験：「派手な成果がなくても、自分のプロセスと能力は手札として通用する」

### 授業の示唆の3層（アプリはこれを支えるためにある）

1. **STEP1-2** … 自分の経験は思っているより手札になる（自己肯定）
2. **STEP3** … これからのカードは自分で発行する（自己決定）
3. **STEP4** … 計画は外れる。そのとき効くのはプロセス・能力・余白。そして**外れても目指し直せる**（レジリエンス）

STEP4の「演出（裏返し）→ 再発行（目指し直し）→ 手札選択（今あるもので向かう）」という順序は授業の核心。
**この順序は変えてはいけない。**

---

## 3. 設計原則

> **「ルールは薄く、対話は厚く、所作は残す」**

- **アプリは判定しない**。勝敗・点数・スコア・ランキング・数値パラメータは一切実装しない
- **所作を大事にする**。カードを「作る・並べる・切る・裏返す」という手の動きをUIで再現する
  （追加時のめくりアニメーション、ドラッグ並べ替え、未来カードの3D裏返し）
- 迷ったら、**機能を足すより所作の質を優先する**
- 高校生向けに平易な日本語。漢字は中学生レベル、専門用語なし
- 派手な装飾より読みやすさ。1画面1タスク

---

## 4. 技術構成

**ビルド不要・サーバー不要・通信なし**の静的3ファイル。GitHub → Vercel で配信している。

```
index.html            アプリ本体（HTML + CSS + Vanilla JS、約1,540行。フレームワーク不使用）
sw.js                 Service Worker（オフライン対応・キャッシュファースト）
html2canvas.min.js    画像保存用（CDNではなくローカル同梱。オフライン要件のため）
README.md             運用向けの説明（先生・配布担当者向け）
APP_OVERVIEW.md       このファイル
```

- データは **すべて `localStorage`**（キー: `tankyu-card-game-v1`）。サーバー・DB・ログインなし
- **個人情報を扱わない**。ニックネームのみ。送信機能は存在しない
- 初回ロード後は**オフラインで完全に動作**する（教室のWi-Fiが不安定でも困らないため）
- レスポンシブ（スマホ縦 〜 タブレット横）。まとめ画面はA4縦1枚の印刷CSSあり

### 重要な定数（`index.html` 冒頭にまとめてある）

```js
PASSWORDS      = { 1:"1111", 2:"1111", 3:"1111", 4:"1111" }  // 画面遷移用（キー＝現在のステップ番号）
ADMIN_PASSWORD = "1111"     // 設定画面のJSONエクスポート/インポート用
DATA_VERSION   = 2          // エクスポートJSONに埋めるデータ形式バージョン
LS_KEY         = "tankyu-card-game-v1"
MAX_HAND       = 5          // 手作りカードの上限（不足分は余白カードで5枚に揃える）
MAX_FUTURE     = 2          // お題1で発行できる未来カードの上限（お題2の再発行は1枚固定）
```

`sw.js` には `CACHE_VERSION`（現在 `"v4"`）がある。**`index.html` を更新したら必ずこれを上げる**こと。
上げないと各端末のキャッシュが更新されず、古い画面のまま授業が始まる。

---

## 5. 画面フロー（6画面・一方向のステップ形式）

画面下部に固定フッターのステップインジケータがある（印刷時は非表示）。

| # | 画面 | 内容 | 想定時間 |
|---|---|---|---|
| 0 | スタート | ニックネーム入力（本名不要） | ─ |
| 1 | **STEP1 棚卸し** | 5カテゴリーを素材にカードを作る | 25分 |
| 2 | **STEP2 手札を眺める** | 手札一覧・並べ替え・気づきメモ・班で見せ合い | 10分 |
| 3 | **STEP3 お題1「大学生、何する？」** | メニュー7項目から2つ選択＋未来カード発行 | 15分 |
| 4 | **STEP4 お題2「思ってたのと違った、どうする？」** | 未来カードが裏返る → 目指し直す → 手札で向かう | 15分 |
| 5 | まとめ | 手札と全回答の一覧。画像保存・印刷 | ─ |

### 遷移のルール

- 「次へ」で進むときだけパスワードを要求する（スタート→STEP1 は不要）
- **「もどる」方向はパスワード不要**
- **一度通過した遷移は再入力不要**（`S.passed` に通過済みステップ番号を保存。リロードしても有効）
- バリデーションが先、パスワードが後。入力不備のときはパスワードモーダルすら出さない

---

## 6. データモデル

### 状態オブジェクト `S`（そのまま localStorage に入る）

```js
{
  nickname: "",            // ニックネーム
  step: 0,                 // 0:スタート / 1〜4:STEP / 5:まとめ
  cards: [],               // カード配列（下記）。手札も未来カードも全部ここに入る
  step2Note: "",           // STEP2の気づきメモ
  step3: { menus: [], why: "", effectiveIds: [] },
                           // menus=選んだメニューのindex(0-6)を2つ / effectiveIds=効くと選んだカードのid
  step4: { playedIds: [], action: "", decl: {} },
                           // playedIds=切ったカードのid / decl={余白カードid: 獲得宣言の文}
  futureFlipped: false,    // STEP4の裏返し演出を実行済みか（再訪時に再生しない）
  passed: [],              // パスワード通過済みのステップ番号
  summaryNote: ""          // 「今日の気づき」（振り返りパートで記入）
}
```

### カード1枚のフィールド

```js
{
  id, type, title, context, category, sourceId, futureType, phase }
```

| フィールド | 説明 |
|---|---|
| `type` | `"seika"`（成果）/ `"process"`（プロセス）/ `"skill"`（能力）/ `"blank"`（余白）/ `"future"`（未来） |
| `title` | ひと言タイトル（20字まで） |
| `context` | ひとことメモ。**能力カードのときだけ「出典カードのタイトル」が入る** |
| `category` | 出身カテゴリー。未来・余白は空 |
| `sourceId` | 能力カードの出典カードのid（能力以外は `null`） |
| `futureType` | 未来カードのみ。`seika` / `process` / `skill`（「将来の成果か、プロセスか、能力か」） |
| `phase` | 未来カードのみ。**`1`＝お題1で発行**（STEP4で裏返る）／**`2`＝お題2で目指し直して再発行** |

> **`phase` と `futureType` は表示の出し分けに使われる。** STEP3にはphase1だけ、STEP4の再発行枠にはphase2だけを出す。
> 未来カードを触る変更をするときは、必ず両ステップの表示を確認すること。

### カード5種の意味

| 種別 | 色 | 記入ルール | 意味 |
|---|---|---|---|
| 成果 | 青 | **名詞で書く** | 外から見える、検証可能。語りの入口 |
| プロセス | 緑 | **動詞で書く** | 行動の事実。失敗も含められる。最も汎用 |
| 能力 | 橙 | **力の名前＋出典（必須）** | 持ち運べる力。出典とセットで強くなる |
| 余白 | 白・点線枠 | 記入なし（白紙のまま持つ） | 新しく獲りに行く宣言として切れる枠 |
| 未来 | 紫の点線枠＋種別色のバンド | 種別＋タイトル＋メモ | まだ実物ではない。これから獲りに行くカード |

---

## 7. バリデーション一覧（受け入れ基準に直結）

**STEP1（手札を確定して次へ）**
- 成果・プロセス・能力を**それぞれ1枚以上**作っていないと進めない
- 手作りカードは**合計5枚まで**（6枚目は作れない）
- **能力カードは出典（既存の成果/プロセスカード）なしでは作成できない**
- 出典になっているカードは削除できない（先に能力カードを消す必要がある）
- カード削除時は確認ダイアログ（「消すと元に戻せません」）
- 確定時、5枚に満たない分が**余白カード**で自動的に埋まる（0〜2枚）

**STEP3**
- メニューを**ちょうど2つ**選ぶまで次へ進めない（2つ選ぶと残りは選択不可になる）
- 未来カード（phase1）を**1枚以上**発行しないと進めない（上限2枚）
- 未来カードは**種別（成果/プロセス/能力）が必須**

**STEP4**
- 新しい未来カード（phase2）の**再発行1枚が必須**
- 切るカードを1枚以上選ぶ
- **余白カードを切るときは「獲得宣言」（未来形の文）の入力が必須**
- 「どう動く？」の入力が必須
- **裏返った未来カードは選択できない**（使えるのは高3夏までの手札と余白だけ）

---

## 8. 先生用の機能（設定モーダル：各画面右上の「⚙ 設定」）

- **データリセット** … 端末使い回し用。「復元できません」の確認あり
- **JSONエクスポート / インポート** … 授業前のバックアップ・テストプレイのデータ回収用
  - 実行には `ADMIN_PASSWORD` が必要
  - エクスポート：`tankyu-save_{ニックネーム}_{YYYYMMDD-HHmm}.json`（`dataVersion` 付きで全状態）
  - インポート：形式検証（必須キー・バージョン）で**不正ファイルは拒否し、現在のデータを変更しない**。適用前に上書き確認
- **テストデータ（4ペルソナ）** … ミナ／ケンタ／アオイ／ユウ。STEP1〜まとめまで通しで確認できるよう、
  手札・お題1の回答・phase1未来カード・phase2未来カード・STEP4回答一式まで入っている（読み込み時に上書き確認あり）

### 4ペルソナの性格（動作確認の観点をカバーしている）

| ペルソナ | 構成 | 確認できること |
|---|---|---|
| ミナ | 探究充実型・手札5枚（余白0） | 余白なしのケース |
| ケンタ | 部活一筋型・手札3枚＋余白2 | 余白＋獲得宣言のケース |
| アオイ | 日常型・手札3枚＋余白2 | 「日常・生活」が手札になるケース |
| ユウ | 学業型・手札4枚＋余白1 | 余白1枚・時間差計画のケース |

---

## 9. 実装の構造と、触るときの地雷

### レンダリング方式

**状態 → `render()` → `innerHTML` 全書き換え**、というシンプルな自作SPA。仮想DOMはない。

```
render() → viewStepN()（HTML文字列を返す）→ afterStepN()（イベント結線）
```

これが以下の注意点の原因になっている。

### ⚠ 地雷1：フォームの入力値は必ず「下書きオブジェクト」に保持する

`render()` が走るとDOMが丸ごと作り直されるため、`<input>` の値は消える。
そのため入力中の値は必ず以下のモジュール変数に同期している。

- `form1` … STEP1のカード作成フォーム（種別・タイトル・メモ・カテゴリー・出典・説明パネルの開閉）
- `form3` … STEP3の未来カード発行フォーム（種別・タイトル・メモ）
- `form4` … STEP4の未来カード再発行フォーム（同上）

**新しい入力欄を足すときは、必ず下書きオブジェクトにも足すこと。** 忘れると
「メニューをチェックしたら書きかけの未来カードが消えた」という実害のあるバグになる（過去に実際に起きた）。

### ⚠ 地雷2：余白カードのIDは維持しなければならない

`finishStep1()` は余白カードを**全消し再生成せず、差分だけ増減する**。
IDが変わると `step4.decl`（獲得宣言）や `step4.playedIds` の参照が孤児になり、
生徒が書いた宣言が静かに消える。STEP1に戻って再確定する操作は実際に起こるので、ここは崩さないこと。

### ⚠ 地雷3：未来カードの `phase` / `futureType` フィルタ

`futuresPhase(1)` / `futuresPhase(2)` で出し分けている。
裏返し演出の対象は phase1 のみ。STEP4の「切るカード」候補に未来カードを含めてはいけない。

### ⚠ 地雷4：画像保存は3D変形を平面化してから撮る

html2canvas はCSSの `rotateY` を再現できない。そのため撮影中だけ `body.capturing` クラスを付け、
裏返しカードを平面化し、ボタン類（`.no-print`）を隠している。失敗時はトーストで
「うまく保存できないときは、スクリーンショットを撮ってね」と案内するフォールバックがある。

### ⚠ 地雷5：`sw.js` の `CACHE_VERSION`

`index.html` を変更したら必ず上げる。上げ忘れると端末に古い版が残る。

### その他の実装メモ

- カードのタップ選択（`.pickable`）と削除（`[data-del]`）は `document` の**委譲クリックハンドラ**で処理している
- ドラッグ並べ替えは Pointer Events。**220msの長押しで発動**させ、横スクロールと共存させている。
  `setPointerCapture` は環境によって例外を投げるので try-catch で保護済み（iPad Safari / Chromebook 想定）
- Service Worker は `http(s)` のときだけ登録する（`file://` では登録できないため）
- `file://` で直接開くと、ブラウザによっては `download` 属性の日本語ファイル名が無視される。
  本番は Vercel 等のHTTP配信なので実害はない

---

## 10. スコープ外（意図的に作っていないもの）

- 班同士のデータ共有・通信機能（共有は**口頭とカード画面の見せ合い**で行う）
- 勝敗判定・スコア・ランキング
- 教員用の集計ダッシュボード（将来課題）
- ログイン・アカウント管理

これらは「作り忘れ」ではなく設計思想として外している。追加を検討するときは
セクション3の設計原則と衝突しないか必ず確認すること。

---

## 11. 開発の経緯（変更履歴の要約）

| 版 | 主な内容 |
|---|---|
| v1 | 初版。5画面フロー、カード3種＋余白、手札構成ルール、未来カードの裏返し、まとめ画面 |
| v2 | ステップ表示をフッター固定に／画面遷移パスワード導入／**STEP4を「目指し直す」構造に再設計**（phase2未来カードの再発行を必須化）／未来カードに種別を追加／オフライン対応（Service Worker）／フォーム下書き・余白ID維持などのバグ修正 |
| v3 | まとめ画面のPNG画像保存／設定画面のJSONエクスポート・インポート／確認ダイアログの整備／4ペルソナにお題2データを追加／未来カードの×ボタンとバンド表記の重なり解消 |
| v3.1 | パスワードを一旦すべて `1111` に統一 |

検証は Playwright による通しテストで行っている（スマホ縦390px想定で全ステップ操作、4ペルソナの通し実行、
オフラインでのリロードと画像保存まで確認済み）。
